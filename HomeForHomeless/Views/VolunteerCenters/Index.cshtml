@using PagedList.Mvc
@model  HomeForHomeless.ViewModel.VCList

@{
    ViewBag.Title = "VolunteerCenter";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<body>
    <div class="col-12 text-center mt-5">
        <h1 class="text-dark pt4">Available Volunteer Centers</h1>
        <div class="border-top border-primary w-25 mx-auto my03"></div>
        <p class="lead">Volunteer Centers near you</p>
    </div>
    <div class="col-12 text-center mt-5">
        <form asp-action="Index" method="get" class="form-inline row justify-content-center">
            <input type="search" class="form-control mr-sm-2" maxlength="256" name="SearchString" value="@ViewData["currentFilter"]" placeholder="Volunteer Centers" id="search" required="" aria-label="Search">
            <button class="btn aqua-gradient btn-rounded btn-sm my-0" type="submit">FIND NOW</button>
        </form>
    </div>

    @* iterative code to display all the appliance in database in grids of cards.  *@

    <div class="container">
        @if (Model != null)
        {
            <div class="row">
                @foreach (var item in Model.VCs)
                {
                    <div class="col-md-4 my-4" style="text-align:center;">
                        <img src="~/Images/favicon.png" alt="" class="w-100">
                        <div class="h4 my-4">
                            @item.Name
                        </div>
                        <p>
                            @item.Address
                        </p>
                        <a href="@Url.Action("Details", "VolunteerCenters",new { id = @item.VolunteerCenter_Id})" class="btn btn-outline-dark btn-md">See More</a>
                    </div>
                }
            </div>
        }
    </div>


    <br>
    <br>

    @* code to display page count based on the data in the database. *@

    <div class="pagecount" style="margin:auto; text-align:center">
        Page @(Model.VCs.PageCount < Model.VCs.PageNumber ? 0 : Model.VCs.PageNumber)
        of @Model.VCs.PageCount   @Html.PagedListPager(Model.VCs, page => Url.Action("Index", new { page, currentfilter = ViewBag.currentfilter }))
    </div>
</body>
